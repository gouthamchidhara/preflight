/**
 * Mock agent CLI — fakes enroll / checkin / runs / job results for dev and tests (PLAN.md T5).
 * TODO(T5): CLI args for device count, readiness state mix, run posting.
 */
export const MOCK_AGENT_NAME = 'mock-agent';
