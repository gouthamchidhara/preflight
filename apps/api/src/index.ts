/**
 * Fastify API entry (PLAN.md §5.4).
 * TODO(T3): Drizzle schema + migrations + manual_items seed (§5.6).
 * TODO(T4): agent endpoints, device-key auth, bootstrap tokens, readiness calc (§5.3).
 * TODO(T6): user endpoints, Entra JWT via JWKS, roles, AUTH_MODE=dev guard (§8).
 */
export const API_NAME = 'umd-validation-api';
