/**
 * App shell (PLAN.md §7).
 * TODO(T7): routes /overview /devices /rules /actions /settings, layout + nav.
 */
export function App() {
  return (
    <main>
      <h1>UMD Validation Console</h1>
      <p>Placeholder shell — routes land in T7.</p>
    </main>
  );
}
