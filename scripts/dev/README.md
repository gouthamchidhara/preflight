# Dev environment

`compose.dev.yml` and bootstrap scripts land here in **T5** (PLAN.md).

They stand up Postgres 16 + the API + the mock agent so the stack runs locally
without any UMD domain membership.
