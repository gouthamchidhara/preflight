// TODO(T3): replace with real Drizzle migrations + seed for manual_items (§5.4, §5.6).
const action = process.argv[2] ?? 'unknown';
console.log(`[placeholder] pnpm db:${action} — real migrations land in T3 (PLAN.md).`);
